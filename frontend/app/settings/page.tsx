"use client";
import { useScenario } from "@/components/layout/ScenarioProvider";
import type { Scenario } from "@/types/dashboard";
export default function SettingsPage() { const { scenario, setScenario, data } = useScenario(); return <><p className="eyebrow">Settings</p><h1 className="mt-1 text-3xl font-extrabold tracking-tight">Demo and worker settings</h1><p className="muted mt-2">Use scenarios to demonstrate how FlowGuard adapts to financial conditions.</p><section className="panel mt-7 max-w-xl"><label className="text-sm font-bold">Demo scenario<select value={scenario} onChange={(event) => setScenario(event.target.value as Scenario)} className="mt-2 w-full rounded-lg border border-[#dfe7e1] bg-white p-3"><option value="NORMAL">Normal</option><option value="INCOME_SHOCK">Income Shock</option><option value="RECOVERY">Recovery</option></select></label><div className="mt-6 border-t border-[#edf1ee] pt-5"><p className="eyebrow">Worker context</p><p className="mt-2 font-bold">{data?.worker.name}</p><p className="muted">Primary income: {data?.worker.occupation} · Average monthly income: ₹24,500</p></div></section></>; }
