"use client";
import { useState } from "react";
import { ShieldCheck } from "lucide-react";
import { formatINR } from "@/lib/formatters";
export default function CreditPage() { const [amount, setAmount] = useState(1100); const [months, setMonths] = useState(4); const payment = Math.ceil(amount / months); return <><p className="eyebrow">Credit Guard</p><h1 className="mt-1 text-3xl font-extrabold tracking-tight">Credit is the last layer of protection</h1><p className="muted mt-2">Before borrowing, FlowGuard checks your resilience and available buffer.</p><div className="mt-7 grid gap-5 lg:grid-cols-2"><section className="panel"><div className="flex items-center gap-3"><ShieldCheck className="text-[#087344]" /><div><p className="eyebrow">Protection check</p><h2 className="mt-1">You requested ₹5,000</h2></div></div><div className="mt-7 space-y-4 text-sm"><Line label="Emergency buffer available" value="₹2,100" /><Line label="Expected shortfall" value="₹3,200" /><Line label="Recommended buffer usage" value="₹2,100" /><Line label="Recommended credit" value="₹1,100" emph /></div><div className="mt-6 rounded-xl bg-[#f1f9f3] p-4 text-sm text-[#087344]"><strong className="block">Use buffer first</strong>Only ₹1,100 of credit is needed after protecting your essentials.</div></section><section className="panel"><p className="eyebrow">Repayment simulator</p><h2 className="mt-1">Estimate your repayment</h2><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Amount borrowed<input value={amount} type="number" min="0" step="100" onChange={(event) => setAmount(Number(event.target.value))} className="mt-2 w-full rounded-lg border border-[#dfe7e1] p-3" /></label><label className="text-sm font-semibold">Repayment period<select value={months} onChange={(event) => setMonths(Number(event.target.value))} className="mt-2 w-full rounded-lg border border-[#dfe7e1] bg-white p-3"><option value={4}>4 months</option><option value={6}>6 months</option><option value={12}>12 months</option></select></label></div><div className="mt-8"><p className="eyebrow">Estimated monthly repayment</p><p className="mt-2 text-4xl font-extrabold text-[#087344]">{formatINR(payment)}</p><p className="muted mt-3">Prototype simulation only — not a real credit approval or loan offer.</p></div></section></div></>; }
function Line({ label, value, emph = false }: { label: string; value: string; emph?: boolean }) { return <div className={`flex justify-between border-b border-[#edf1ee] pb-3 ${emph ? "text-[#087344]" : ""}`}><span>{label}</span><strong>{value}</strong></div>; }
