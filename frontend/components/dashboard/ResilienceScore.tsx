export function ResilienceScore({ score, days }: { score: number; days: number }) { const status = score >= 70 ? "STABLE" : score >= 55 ? "WATCH" : "SHOCK"; const color = status === "SHOCK" ? "#b93a3a" : status === "WATCH" ? "#b66b0b" : "#23aa6b"; return <section className="panel"><p className="eyebrow">Financial health</p><div className="mt-4 flex items-center gap-5"><div className="grid h-24 w-24 place-items-center rounded-full" style={{ background: `conic-gradient(${color} ${score}%, #edf2ee 0)` }}><div className="grid h-[74px] w-[74px] place-items-center rounded-full bg-white text-xl font-extrabold">{score}</div></div><div><p className="text-lg font-bold">{status}</p><p className="muted">{days} resilience days available</p></div></div></section>; }
