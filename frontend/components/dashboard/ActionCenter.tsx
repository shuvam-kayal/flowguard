import { ArrowRight, ShieldAlert } from "lucide-react";
import type { Recommendation } from "@/types/dashboard";
export function ActionCenter({ recommendations, shock }: { recommendations: Recommendation[]; shock: boolean }) { return <section className={`rounded-2xl border p-6 ${shock ? "border-[#efc5c5] bg-[#fff6f6]" : "border-[#d5eadb] bg-[#f5fbf6]"}`}><div className="flex gap-3"><ShieldAlert className={shock ? "text-[#b93a3a]" : "text-[#087344]"} /><div><p className="eyebrow">Action center</p><h2 className="mt-1">{shock ? "Income dip expected in 8 days" : "Your next best actions"}</h2></div></div><div className="mt-4 space-y-3">{recommendations.map((item) => <div key={item.title}><p className="text-sm font-bold">{item.title}</p><p className="text-xs text-[#718078]">{item.description}</p></div>)}</div><button className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-[#087344]">Review plan <ArrowRight size={16} /></button></section>; }
