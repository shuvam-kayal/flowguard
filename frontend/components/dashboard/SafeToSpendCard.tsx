"use client";
import { useState } from "react";
import { CircleHelp, X } from "lucide-react";
import { formatINR } from "@/lib/formatters";
export function SafeToSpendCard({ amount, balance }: { amount: number; balance: number }) { const [open, setOpen] = useState(false); return <><section className="relative overflow-hidden rounded-2xl bg-[#087344] p-6 text-white sm:p-8"><p className="eyebrow !text-[#b9e6c8]">Safe to spend today</p><p className="mt-7 text-5xl font-extrabold tracking-[-.07em] sm:text-6xl">{formatINR(amount)}</p><p className="mt-2 text-sm text-[#d9f4e3]">Recommended daily discretionary spending</p><button onClick={() => setOpen(true)} className="mt-8 inline-flex items-center gap-2 text-xs font-bold text-[#d9f4e3] underline underline-offset-4"><CircleHelp size={15} />How is this calculated?</button></section>{open && <div role="dialog" aria-modal="true" className="fixed inset-0 z-30 grid place-items-center bg-[#102017]/40 p-4"><div className="w-full max-w-md rounded-2xl bg-white p-6 shadow-xl"><div className="flex justify-between"><h2>Safe-to-spend plan</h2><button aria-label="Close calculation" onClick={() => setOpen(false)}><X size={20} /></button></div><div className="mt-5 space-y-3 text-sm"><Row label="Current balance" value={formatINR(balance)} /><Row label="Upcoming obligations" value="−₹8,000" /><Row label="Required emergency buffer" value="−₹2,000" /><hr /><Row label="Safe discretionary balance" value={formatINR(Math.max(0, balance - 10000))} strong /><p className="muted pt-2">FlowGuard spreads the safe balance across the days before your next expected income.</p></div></div></div>}</> }
function Row({ label, value, strong }: { label: string; value: string; strong?: boolean }) { return <div className={`flex justify-between ${strong ? "font-bold" : ""}`}><span>{label}</span><span>{value}</span></div>; }
